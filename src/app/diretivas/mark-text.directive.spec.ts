import { MarkTextDirective } from './mark-text.directive';

describe('MarkTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkTextDirective();
    expect(directive).toBeTruthy();
  });
});
